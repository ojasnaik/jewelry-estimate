# jewelry-estimate

An AI-powered jewelry valuation web application. Users upload photos and enter details about a piece of jewelry — metal type, karat, weight, gemstone, and condition — and receive an estimated resale market value range generated by an LLM.

---

## Features

- **Email authentication** — sign up, sign in, email confirmation via Supabase Auth
- **Jewelry intake form** — metal type, karat (dynamic options per metal), weight, gemstone, condition, optional photo upload
- **LLM valuation** — Groq API (llama-3.3-70b-versatile) estimates a USD resale range with appraiser reasoning
- **Real-time status polling** — pending → processing → complete, with auto-refresh on completion
- **Valuation history dashboard** — table of past valuations with status badges and estimated ranges
- **Signed image URLs** — photos served via short-lived Supabase Storage signed URLs, never exposed directly
- **Error tracking** — Sentry captures exceptions with user context, never logging sensitive data
- **Route protection** — proxy (middleware) redirects unauthenticated users away from `/dashboard`, authenticated users away from `/auth`

---

## Stack

| Layer | Technology |
|---|---|
| Framework | Next.js 16 App Router (TypeScript strict) |
| Auth & Database | Supabase (Auth, PostgreSQL, Storage) |
| LLM | Groq API — llama-3.3-70b-versatile |
| Styling | Tailwind CSS v4 |
| Error tracking | Sentry |
| Hosting | Vercel |

---

## Project Structure

```
src/
├── app/
│   ├── auth/
│   │   ├── login/page.tsx        # Sign-in form
│   │   ├── signup/page.tsx       # Registration form
│   │   └── callback/route.ts     # OAuth / magic-link callback
│   ├── dashboard/
│   │   ├── layout.tsx            # Nav bar, session guard, Sentry user context
│   │   ├── page.tsx              # Valuation history table
│   │   ├── new/page.tsx          # New valuation form page
│   │   └── valuations/[id]/page.tsx  # Result / pending / error view
│   ├── api/
│   │   └── valuations/
│   │       ├── route.ts              # POST — create valuation
│   │       ├── process/route.ts      # POST — call Groq, update DB (internal)
│   │       ├── [id]/status/route.ts  # GET — poll status
│   │       └── [id]/image/route.ts   # GET — generate signed storage URL
│   ├── global-error.tsx          # Global error boundary (Sentry + friendly UI)
│   └── page.tsx                  # Root redirect (→ /dashboard or /auth/login)
├── components/
│   ├── ValuationForm.tsx          # Controlled form with image upload
│   ├── ValuationPending.tsx       # Polling spinner, auto-refresh on complete
│   ├── ValuationResult.tsx        # Price display, collapsible reasoning, image
│   └── SignOutButton.tsx          # Client component for Supabase signOut
├── lib/
│   ├── auth/requireAuth.ts        # Centralised API route auth utility
│   └── supabase/
│       ├── client.ts              # Browser Supabase client (NEXT_PUBLIC_ only)
│       └── server.ts              # Server Supabase client (cookies)
├── types/
│   ├── supabase.ts                # Generated Database types
│   └── valuation.ts               # ValuationType = Tables<'valuations'>
└── proxy.ts                       # Session refresh + route protection (Next.js 16)
```

---

## Database Schema

```sql
create table valuations (
  id              uuid primary key default gen_random_uuid(),
  user_id         uuid references auth.users not null,
  status          text not null default 'pending',  -- pending | processing | complete | error
  metal_type      text,
  karat           text,
  weight_grams    numeric,
  gemstone_type   text,
  gemstone_carat  numeric,
  condition       text check (condition in ('excellent','good','fair','poor')),
  image_path      text,
  estimated_low   numeric,
  estimated_high  numeric,
  llm_reasoning   text,
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now()
);
```

Row Level Security is enabled. Users can only read and write their own rows.

---

## Environment Variables

Create a `.env.local` file at the project root:

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
GROQ_API_KEY=
PROCESS_SECRET=
SENTRY_DSN=
```

**Security rules enforced in code:**
- `SUPABASE_SERVICE_ROLE_KEY` is only used in `api/valuations/process/route.ts` (server-to-server)
- `GROQ_API_KEY` is only used in `api/valuations/process/route.ts`
- Client Components only read `NEXT_PUBLIC_` prefixed variables

---

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The root redirects to `/auth/login` for unauthenticated users.

---

## Key Architecture Decisions

**`requireAuth` utility** — all user-facing API routes call `requireAuth(request)` at the top of the handler. It returns the verified `user` and `supabase` client together (one client per request), using a discriminated union so TypeScript enforces the early-return guard at the call site.

**Fire-and-forget processing** — `POST /api/valuations` inserts the row and immediately returns `{ id, status: 'pending' }`. It fires a non-awaited `fetch` to `/api/valuations/process` with an `x-process-secret` header. The processing route calls Groq and updates the DB asynchronously, which allows the UI to navigate to the result page immediately and poll for completion.

**`getUser()` everywhere** — `getSession()` is never used. `getUser()` re-validates the JWT with Supabase servers on every call, preventing spoofed cookie attacks.

**`proxy.ts`** — Next.js 16 renamed `middleware.ts` to `proxy.ts` and the exported function from `middleware` to `proxy`. The file refreshes the session on every request and enforces route-level auth redirects.

**`force-dynamic` on the result page** — the valuation detail page (`/dashboard/valuations/[id]`) exports `dynamic = 'force-dynamic'` so that `router.refresh()` called by `ValuationPending` always triggers a fresh DB read rather than hitting a cached render.

---

## Claude Code Chat History

This project was built using Claude Code. Below is a condensed record of the prompts used.

---

### 1 — Supabase client factories

> Create the Supabase client factory files.
> - `src/types/supabase.ts` — placeholder `Database = any` type
> - `src/lib/supabase/client.ts` — browser singleton using `createBrowserClient` from `@supabase/ssr`, typed with Database generic
> - `src/lib/supabase/server.ts` — server client using `createServerClient` with Next.js `cookies()` from `next/headers`
>
> Use the official `@supabase/ssr` pattern. Do NOT use `@supabase/auth-helpers-nextjs`. Do NOT import `SUPABASE_SERVICE_ROLE_KEY` in the client file.

**Claude:** Created three files. The server client is `async` because `cookies()` is async in Next.js 15+. The `setAll` in server.ts silently swallows errors when called from a Server Component (read-only cookies) — middleware handles session refresh instead.

---

### 2 — Proxy / Middleware

> Create `src/proxy.ts` (Next.js 16 middleware) that:
> - Refreshes the session on every request using the Supabase SSR cookie pattern
> - Redirects unauthenticated users accessing `/dashboard` to `/auth/login`
> - Redirects authenticated users accessing `/auth/*` to `/dashboard`
> - Uses `getUser()` not `getSession()`
> - Skips static files via matcher config

**Claude:** Built the client inline using `NextRequest`/`NextResponse` directly — the `cookies()` helper from `next/headers` is not available in middleware. The function was later renamed from `middleware` to `proxy` when Next.js 16 changed the convention.

---

### 3 — Authentication UI

> Build the full auth UI:
> - `src/app/auth/login/page.tsx` — email/password, Eye/EyeOff toggle, friendly error mapping, spinner, redirect to `/dashboard` on success
> - `src/app/auth/signup/page.tsx` — email/password/confirm, client-side validation (min 8 chars, match), "Check your email" success screen
> - `src/app/auth/callback/route.ts` — exchanges OAuth/magic-link code for session, redirects to `/dashboard` or `/auth/login`
>
> Tailwind only. Gold `#C9A84C` primary button. No form libraries.

---

### 4 — Root redirect

> The root route `/` does not redirect. Fix `src/app/page.tsx` to redirect authenticated users to `/dashboard` and unauthenticated users to `/auth/login` using `getUser()`.

---

### 5 — ValuationForm component

> Build `src/components/ValuationForm.tsx`:
> - Fields: metal type, karat (options change per metal), weight (grams), gemstone type, gemstone carat (conditional), condition (radio), image upload (5 MB limit, preview thumbnail)
> - On submit: upload image to Supabase Storage at `{userId}/{uuid}-{filename}`, POST form data + `image_path` to `/api/valuations`, navigate to `/dashboard/valuations/{id}`
> - Inline field errors, loading/disabled state
> - No form libraries, `useState` only

---

### 6 — API routes

> Create two API routes:
>
> `POST /api/valuations` — verify session with `getUser()`, validate required fields, insert row with `status='pending'`, fire-and-forget POST to `/api/valuations/process` with `x-process-secret` header, return `{ id, status }` with 201.
>
> `POST /api/valuations/process` — check `x-process-secret` header first, fetch valuation using service role client (bypasses RLS), set status to `'processing'`, call Groq API (`llama-3.3-70b-versatile`, max_tokens 400, temperature 0.1), strip markdown fences before `JSON.parse()`, update row with estimates and `status='complete'`. On any error: set `status='error'`, call `Sentry.captureException` with `{ extra: { valuationId } }`.

---

### 7 — Valuation result page and sub-components

> Create:
> - `src/app/dashboard/valuations/[id]/page.tsx` — server component, ownership check, renders `<ValuationPending>`, `<ValuationResult>`, or error card based on status
> - `src/components/ValuationPending.tsx` — animated spinner, polls `/api/valuations/{id}/status` every 3 seconds, calls `router.refresh()` on complete, shows error state without polling on failure
> - `src/components/ValuationResult.tsx` — formatted USD range, mid-point estimate, collapsible reasoning, signed image URL fetched from `/api/valuations/{id}/image`, jewelry summary card
> - `GET /api/valuations/[id]/status` — session + ownership check, returns `{ status, estimated_low, estimated_high }`
> - `GET /api/valuations/[id]/image` — session + ownership check, generates 60-second signed Supabase Storage URL

---

### 8 — Connection point audit

> Review all integration points for gaps and mismatches. Check: `PROCESS_SECRET` header present in fire-and-forget call, storage bucket name consistent, polling interval cleared on unmount, TypeScript types consistent, JSON fence stripping, `force-dynamic` on result page.

**Claude found and fixed:**
- Storage bucket was `'valuations'` in two places — changed to `'jewelry-images'` to match the actual bucket
- Result page missing `export const dynamic = 'force-dynamic'` — `router.refresh()` would return a cached render
- Unused `ext` variable in `ValuationForm` (TypeScript strict error)
- `stripJsonFences` regex didn't handle leading whitespace before the opening fence
- `request.json()` in `process/route.ts` was outside `try/catch` — a bad body would skip Sentry and the `status='error'` update

---

### 9 — Dashboard and layout

> Build the authenticated section:
> - `src/app/dashboard/layout.tsx` — session guard, nav bar with logo, user email, sign-out button, `Sentry.setUser()`
> - `src/app/dashboard/page.tsx` — last 10 valuations, color-coded status badges (gold=complete, grey=pending/processing, red=error), estimated range, View links, empty state
> - `src/app/dashboard/new/page.tsx` — fetches user ID server-side, renders `<ValuationForm>`
> - `src/components/SignOutButton.tsx` — client component, calls `supabase.auth.signOut()` then navigates to `/auth/login`

---

### 10 — Bug fix: condition check constraint

> Submitting the form returned 500. The Supabase `valuations` table has a `CHECK` constraint requiring lowercase condition values (`excellent`, `good`, `fair`, `poor`). The form was sending `'Excellent'` etc.

**Claude:** Refactored `CONDITIONS` in `ValuationForm` to separate `label` (display) and `value` (submitted), keeping the UI unchanged while sending lowercase to the DB.

---

### 11 — Sentry error capture

> Add `Sentry.captureException` to:
> - `ValuationForm.tsx` catch block — error only, no form data
> - `global-error.tsx` — replace auto-generated `NextError` placeholder with a friendly "Something went wrong" screen and reload button
>
> (Process route and dashboard layout already had Sentry wired correctly.)

---

### 12 — Security audit

> Audit all files for: `SUPABASE_SERVICE_ROLE_KEY` in client files, `GROQ_API_KEY` outside API routes, non-`NEXT_PUBLIC_` env vars in Client Components, missing `getUser()` before DB operations, missing `x-process-secret` check, signed URL generation in client code, `getSession()` usage in proxy.

**Result: all 7 checks passed.** No fixes required.

---

### 13 — `requireAuth` utility

> Create `src/lib/auth/requireAuth.ts` — centralised session verification for all API routes. Accept the request, call `getUser()`, return a discriminated union of `{ user, supabase, response: null }` on success or `{ response: NextResponse(401) }` on failure. Update all user-facing API routes to use it.

**Claude:** Returned the `supabase` client alongside the user so routes don't create a second client. The discriminated union means TypeScript enforces the `if (auth.response) return auth.response` guard — forgetting it causes a type error when accessing `auth.user`. The `process/route.ts` route was intentionally excluded (server-to-server, guarded by `x-process-secret` instead).
